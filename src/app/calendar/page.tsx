import Link from "next/link";
import { ArrowLeft, ArrowRight, CalendarDays } from "lucide-react";
import { events, formatDate } from "@/lib/events";

export default function CalendarPage() {
  const month = new Intl.DateTimeFormat("en-US", { month: "long", year: "numeric" }).format(new Date("2026-10-01T00:00:00"));
  return <div className="page-shell"><section className="directory-header"><div><p className="section-kicker">Plan your season</p><h1>Race calendar.</h1><p>See what is coming up, then make a plan worth training for.</p></div><div className="calendar-toggle"><button className="active"><CalendarDays size={16} /> Calendar</button><button>List</button></div></section><section className="calendar-board"><div className="calendar-board__header"><button aria-label="Previous month"><ArrowLeft size={17} /></button><h2>{month}</h2><button aria-label="Next month"><ArrowRight size={17} /></button></div><div className="calendar-weekdays">{["Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun"].map((day) => <span key={day}>{day}</span>)}</div><div className="calendar-grid">{Array.from({ length: 31 }, (_, index) => { const day = index + 1; const items = events.filter((event) => Number(event.date.slice(-2)) === day); return <div className={`calendar-day ${items.length ? "has-events" : ""}`} key={day}><span>{day}</span>{items.map((event) => <Link href={`/events/${event.slug}`} key={event.id}>{event.title}</Link>)}</div>; })}</div></section><section className="calendar-list"><div className="section-heading"><div><p className="section-kicker">On the horizon</p><h2>October starts here.</h2></div></div>{events.filter((event) => event.date.startsWith("2026-10")).map((event) => <div className="calendar-list__item" key={event.id}><span>{formatDate(event.date)}</span><Link href={`/events/${event.slug}`}>{event.title}</Link><span>{event.city}, {event.country}</span></div>)}</section></div>;
}
